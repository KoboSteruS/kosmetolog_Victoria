# 🚀 Быстрая установка и запуск

## Пошаговая инструкция для Windows

### Шаг 1: Установка Python

1. Скачайте Python 3.10+ с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Проверьте установку:
```cmd
python --version
```

### Шаг 2: Подготовка проекта

1. Откройте командную строку в папке проекта `C:\projects\Victoria`
2. Создайте виртуальное окружение:
```cmd
python -m venv venv
```

3. Активируйте виртуальное окружение:
```cmd
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```cmd
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта
2. Скопируйте содержимое из `.env.example`
3. Измените `SECRET_KEY` на случайную строку

### Шаг 5: Добавление изображений

1. Положите логотипы в `app/static/images/`:
   - `logo.png` - основной логотип
   - `logo-light.png` - светлый логотип
   - `hero-bg.jpg` - фон для первого экрана

2. Добавьте фото специалистов:
   - `doctor1.jpg`, `doctor2.jpg`, `doctor3.jpg`, `doctor4.jpg`

3. Добавьте фото клиники:
   - `clinic1.jpg`, `clinic2.jpg`, `clinic3.jpg`

### Шаг 6: Запуск приложения

```cmd
python run.py
```

### Шаг 7: Открыть в браузере

Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

## 🎉 Готово!

Ваш лендинг запущен и готов к работе!

## ⚙️ Дополнительные настройки

### Изменить порт

В файле `.env` измените:
```env
APP_PORT=8000
```

### Использовать PostgreSQL

1. Установите PostgreSQL
2. Создайте базу данных
3. В `.env` измените:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/victoria_db
```

### Отключить debug режим

В файле `.env`:
```env
DEBUG=False
FLASK_ENV=production
```

## 🐛 Решение проблем

### Ошибка "Python not found"

Убедитесь что Python добавлен в PATH

### Ошибка при установке пакетов

Попробуйте обновить pip:
```cmd
python -m pip install --upgrade pip
```

### Порт уже занят

Измените порт в `.env` или остановите процесс на порту 5000

### База данных не создается

Проверьте права на запись в папке проекта

## 📞 Поддержка

Если возникли проблемы, проверьте логи в папке `logs/`

