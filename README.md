# 🌸 Клиника косметологии "Виктория"

Современный одностраничный лендинг для клиники косметологии, разработанный на Flask с использованием лучших практик разработки.

## 📋 Описание проекта

Профессиональный лендинг для клиники косметологии "Виктория" с полным функционалом записи на процедуры, отзывами клиентов и информацией об услугах. Приложение построено с учетом принципов SOLID, DRY и современных паттернов проектирования.

### ✨ Основные возможности

- 🎨 Современный дизайн в розово-пудровой цветовой гамме
- 📱 Адаптивная верстка для всех устройств
- 📝 Форма записи на консультацию с валидацией
- ⭐ Система отзывов клиентов с рейтингом
- 🔍 Фильтрация услуг по категориям
- 💼 Информация о специалистах клиники
- 📄 Раздел с правовыми документами
- 🗺️ Интеграция с картами
- 📧 API для обработки заявок
- 🔐 Валидация данных через Pydantic
- 📊 Логирование через Loguru

## 🎨 Цветовая палитра

```css
--primary-color: #C87B85     /* Розово-пудровый (основной акцентный) */
--secondary-color: #F5E9E9   /* Светло-розовый (вторичный фон) */
--dark-color: #2C1B1B        /* Темно-коричневый (текст/фон) */
--white-color: #FFFFFF       /* Белый (фон и текст) */
```

## 🏗️ Структура проекта

```
Victoria/
├── app/
│   ├── __init__.py                 # Инициализация приложения
│   ├── config/
│   │   ├── __init__.py
│   │   └── settings.py             # Настройки приложения (Pydantic)
│   ├── models/
│   │   ├── __init__.py
│   │   ├── base.py                 # Базовая модель с uuid, timestamps
│   │   ├── appointment.py          # Модель заявки на запись
│   │   └── review.py               # Модель отзыва
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── appointment.py          # Pydantic схемы для заявок
│   │   └── review.py               # Pydantic схемы для отзывов
│   ├── views/
│   │   ├── __init__.py
│   │   └── main.py                 # Views и API endpoints
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css           # Основные стили
│   │   ├── js/
│   │   │   └── main.js             # JavaScript логика
│   │   └── images/                 # Изображения
│   └── templates/
│       ├── base.html               # Базовый шаблон
│       └── index.html              # Главная страница
├── logs/                           # Логи приложения
├── requirements.txt                # Зависимости Python
├── .env.example                    # Пример файла окружения
├── run.py                          # Точка входа приложения
└── README.md                       # Документация

```

## 🚀 Установка и запуск

### Требования

- Python 3.10+
- pip

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Victoria
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
# Flask Configuration
SECRET_KEY=your-super-secret-key-change-me
FLASK_ENV=development
DEBUG=True

# Database Configuration
DATABASE_URL=sqlite:///victoria.db

# Application Settings
APP_NAME=Victoria Clinic
APP_HOST=0.0.0.0
APP_PORT=5000
```

### 5. Инициализация базы данных

База данных создается автоматически при первом запуске приложения.

### 6. Запуск приложения

```bash
python run.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 📚 API Endpoints

### Заявки на запись

**POST /api/appointments**

Создание заявки на консультацию.

**Request Body:**
```json
{
  "name": "Анна Иванова",
  "phone": "+79991234567",
  "service": "Биоревитализация лица",
  "comment": "Хочу записаться на 15:00",
  "agreed_to_processing": true,
  "agreed_to_newsletter": false
}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время.",
  "data": {
    "uuid": "123e4567-e89b-12d3-a456-426614174000",
    "name": "Анна Иванова",
    "phone": "+79991234567",
    "service": "Биоревитализация лица",
    "status": "new"
  }
}
```

### Отзывы

**POST /api/reviews**

Создание отзыва клиента.

**Request Body:**
```json
{
  "name": "Мария Петрова",
  "rating": 5,
  "text": "Отличная клиника! Очень довольна результатом процедуры биоревитализации."
}
```

**GET /api/reviews**

Получение списка опубликованных отзывов.

**Response:**
```json
{
  "success": true,
  "data": [
    {
      "uuid": "123e4567-e89b-12d3-a456-426614174001",
      "name": "Мария Петрова",
      "rating": 5,
      "text": "Отличная клиника!",
      "is_published": true
    }
  ]
}
```

## 🔧 Конфигурация

### Настройки приложения

Все настройки находятся в `app/config/settings.py` и загружаются из переменных окружения через Pydantic Settings.

### База данных

По умолчанию используется SQLite. Для продакшна рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/victoria_db
```

### Логирование

Логи сохраняются в папке `logs/` с ротацией по дням и автоматическим сжатием. Хранятся 30 дней.

## 🧪 Тестирование

### Подход к тестированию

Для тестирования рекомендуется использовать pytest:

```bash
pip install pytest pytest-flask pytest-cov
```

### Ключевые тест-кейсы

1. **Модели:**
   - Создание записей с правильными полями
   - Проверка валидации данных
   - Работа с timestamps

2. **API Endpoints:**
   - Успешное создание заявки
   - Валидация входных данных
   - Обработка ошибок
   - Проверка форматов ответов

3. **Схемы Pydantic:**
   - Валидация телефона
   - Обязательные поля
   - Ограничения длины строк

4. **Edge Cases:**
   - Некорректный формат телефона
   - Отсутствие согласия на обработку ПДн
   - Пустые обязательные поля
   - SQL injection попытки
   - XSS атаки

### Пример теста

```python
def test_create_appointment(client):
    """Тест создания заявки на запись"""
    response = client.post('/api/appointments', json={
        'name': 'Test User',
        'phone': '+79991234567',
        'agreed_to_processing': True
    })
    
    assert response.status_code == 201
    assert response.json['success'] is True
```

## 📈 Масштабируемость

### Горизонтальное масштабирование

1. **Использование внешней БД:**
   - PostgreSQL для надежности
   - Пул соединений через SQLAlchemy

2. **Кэширование:**
   - Redis для кэширования часто запрашиваемых данных
   - Кэширование списка услуг и специалистов

3. **Балансировка нагрузки:**
   - Nginx в качестве reverse proxy
   - Несколько экземпляров Flask за Gunicorn

4. **Асинхронные задачи:**
   - Celery для отправки email уведомлений
   - Redis как брокер сообщений

### Потенциальные узкие места

1. **База данных:**
   - При большом количестве заявок нужны индексы
   - Архивирование старых записей

2. **Статические файлы:**
   - CDN для изображений
   - Оптимизация и сжатие

3. **API Rate Limiting:**
   - Защита от спама через Flask-Limiter

## 🔐 Безопасность

- ✅ CSRF защита через Flask-WTF
- ✅ SQL Injection защита через SQLAlchemy ORM
- ✅ XSS защита через Jinja2 автоэкранирование
- ✅ Валидация входных данных через Pydantic
- ✅ Секретные ключи в переменных окружения
- ⚠️ Рекомендуется добавить SSL в продакшне
- ⚠️ Рекомендуется добавить rate limiting

## 🚀 Деплой в продакшн

### 1. Использование Gunicorn

```bash
gunicorn -w 4 -b 0.0.0.0:5000 run:app
```

### 2. Systemd Service

Создайте файл `/etc/systemd/system/victoria.service`:

```ini
[Unit]
Description=Victoria Clinic Flask App
After=network.target

[Service]
User=www-data
WorkingDirectory=/var/www/victoria
Environment="PATH=/var/www/victoria/venv/bin"
ExecStart=/var/www/victoria/venv/bin/gunicorn -w 4 -b 127.0.0.1:5000 run:app

[Install]
WantedBy=multi-user.target
```

### 3. Nginx конфигурация

```nginx
server {
    listen 80;
    server_name victoria-clinic.ru;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /static {
        alias /var/www/victoria/app/static;
        expires 30d;
    }
}
```

## 📝 Список услуг

Клиника предлагает следующие услуги:

- **Косметология:** Чистки лица, уходы, маски
- **Пилинги:** Поверхностные, срединные, карбоновые, алмазные
- **Массажи:** Ручной массаж лица, LPG-массаж, вакуумный массаж
- **Инъекционные процедуры:** Биоревитализация, мезотерапия, карбокситерапия
- **Аппаратная косметология:** RF-лифтинг, фототерапия, лазерная эпиляция, прессотерапия

Полный прайс-лист доступен на сайте.

## 👥 Команда разработки

Проект разработан с соблюдением профессиональных стандартов:

- Модульная архитектура
- Чистый код
- Документация
- Логирование
- Обработка ошибок

## 📄 Лицензия

Все права защищены © 2024 Клиника косметологии "Виктория"

## 📞 Контакты

- **Телефон:** +7 (999) 123-45-67
- **Email:** info@victoria-clinic.ru
- **Адрес:** г. Москва, ул. Примерная, д. 1

---

**Commit message для git:**
```
feat: добавлен полнофункциональный лендинг клиники косметологии

- Реализована модульная структура Flask приложения
- Добавлены модели БД с BaseModel (uuid, timestamps)
- Созданы Pydantic схемы для валидации форм
- Реализованы API endpoints для заявок и отзывов
- Добавлен современный адаптивный дизайн
- Настроено логирование через loguru
- Добавлена документация и инструкции по развертыванию
```

