# Управление отзывами

## 📝 Как это работает

### Автоматическая публикация
Начиная с последнего обновления, все **новые** отзывы автоматически публикуются (`is_published=True`).

### Старые отзывы
Отзывы созданные **до** обновления остались неопубликованными (`is_published=False`).

## 🚀 Опубликовать все старые отзывы

Запустите скрипт:
```bash
python publish_all_reviews.py
```

Вы увидите:
```
📋 Найдено 1 неопубликованных отзывов
  ✅ Опубликован отзыв от РОМАН (5★)
🎉 Успешно опубликовано 1 отзывов!

📊 Статистика:
   Всего отзывов: 1
   Опубликовано: 1
   Скрыто: 0
```

## 🔧 API endpoints для модерации

### 1. Получить ВСЕ отзывы (включая скрытые)
```bash
GET http://localhost:5000/api/reviews/all
```

Ответ:
```json
{
  "success": true,
  "total": 5,
  "published": 4,
  "unpublished": 1,
  "data": [
    {
      "uuid": "cb93794a-da67-455d-a0ce-18488749d88b",
      "name": "РОМАН",
      "rating": 5,
      "text": "Отличная клиника!",
      "is_published": false,
      "created_at": "2025-12-25T16:42:10"
    }
  ]
}
```

### 2. Опубликовать отзыв
```bash
POST http://localhost:5000/api/reviews/{uuid}/publish
```

Пример:
```bash
curl -X POST http://localhost:5000/api/reviews/cb93794a-da67-455d-a0ce-18488749d88b/publish
```

### 3. Скрыть отзыв
```bash
POST http://localhost:5000/api/reviews/{uuid}/unpublish
```

Пример:
```bash
curl -X POST http://localhost:5000/api/reviews/cb93794a-da67-455d-a0ce-18488749d88b/unpublish
```

## 📊 Проверить опубликованные отзывы

### На сайте
Откройте главную страницу - секция "Отзывы"

### Через API
```bash
GET http://localhost:5000/api/reviews
```

Вернет только **опубликованные** отзывы.

## 💡 Рекомендации

### Для автоматической публикации (текущая настройка)
✅ Все новые отзывы сразу видны на сайте  
❌ Нет модерации

**Подходит если:** доверяете клиентам или хотите максимальную прозрачность

### Для модерации
Измените в `app/models/review.py`:
```python
is_published = db.Column(
    db.Boolean,
    default=False,  # Требуется модерация
    nullable=False,
    comment="Опубликован ли отзыв"
)
```

После этого:
1. Отзывы создаются скрытыми
2. Проверяйте через `/api/reviews/all`
3. Публикуйте вручную через `/api/reviews/{uuid}/publish`

## 🎯 Быстрое решение текущей проблемы

1. **Опубликуйте все старые отзывы:**
   ```bash
   python publish_all_reviews.py
   ```

2. **Обновите страницу сайта** (Ctrl+F5)

3. **Готово!** Все отзывы теперь видны.

## 🔮 Будущие улучшения

- Админ-панель для управления отзывами через веб-интерфейс
- Уведомления в Telegram о новых отзывах
- Возможность отвечать на отзывы
- Фильтрация по рейтингу
- Модерация с причинами отклонения

