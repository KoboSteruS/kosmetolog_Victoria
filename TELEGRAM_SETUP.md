# Настройка Telegram бота для уведомлений

## 🤖 О интеграции

Все заявки с сайта автоматически отправляются в Telegram всем пользователям, которые начали чат с ботом.

## 📋 Инструкция по настройке

### 1. Бот уже создан
Токен бота: `8588808155:AAHHfeIWRADTgRcOdSDI9byUHmzPJE2LGA8`

### 2. Как начать получать уведомления

Любой человек (администратор, менеджер) должен:

1. Открыть бота в Telegram
2. Написать боту команду `/start` или любое сообщение
3. После этого бот автоматически начнет отправлять уведомления о новых заявках

**Важно:** Бот автоматически определяет всех пользователей, которые с ним взаимодействовали, и отправляет им уведомления.

### 3. Формат уведомлений

При поступлении новой заявки бот отправляет сообщение:

```
🆕 Новая заявка на запись!

👤 Имя: Иван Иванов
📱 Телефон: +7 (999) 123-45-67
💅 Услуга: Биоревитализация лица
💬 Комментарий: Хочу записаться на эту неделю

📅 Не забудьте связаться с клиентом!
```

## 🔧 Технические детали

### Как это работает

1. Пользователь заполняет форму на сайте
2. Заявка сохраняется в базу данных
3. Система получает список всех chat_id пользователей, которые писали боту
4. Отправляет уведомление всем этим пользователям одновременно

### Файлы интеграции

- `app/services/telegram_service.py` - сервис для работы с Telegram API
- `app/views/main.py` - интеграция отправки при создании заявки
- `app/config/settings.py` - настройки токена бота

### API методы

- `get_updates()` - получает список всех чатов с ботом
- `send_message(chat_id, text)` - отправляет сообщение в конкретный чат
- `send_to_all(text)` - отправляет всем активным пользователям
- `format_appointment_message()` - форматирует уведомление о заявке

## 🚀 Преимущества

✅ Мгновенные уведомления о новых заявках  
✅ Несколько сотрудников могут получать уведомления  
✅ Не требует постоянной проверки сайта  
✅ Работает 24/7 автоматически  
✅ Не нужно вручную добавлять chat_id - система сама определяет активных пользователей

## 📱 Проверка работы

### Шаг 1: Напишите боту
1. Откройте бота в Telegram
2. Отправьте команду `/start` или любое сообщение
3. Бот должен быть активен (создан через @BotFather)

### Шаг 2: Проверьте что бот видит ваш chat_id
Откройте в браузере:
```
http://localhost:5000/api/telegram/test
```

Вы увидите JSON с информацией:
- `active_chats` - список ваших chat_id
- `active_chats_count` - сколько активных чатов
- `recent_updates` - последние сообщения боту

**Если `active_chats` пустой:**
1. Напишите боту `/start` еще раз
2. Обновите страницу `/api/telegram/test`
3. Если всё еще пустой - добавьте chat_id вручную в настройки (см. ниже)

### Шаг 3: Создайте тестовую заявку
1. Создайте заявку на сайте
2. Проверьте логи приложения - должна быть строка "Уведомление отправлено в Telegram"
3. Проверьте Telegram - должно прийти уведомление

## 🔧 Если бот не отправляет уведомления

### Вариант 1: Добавить chat_id вручную

1. Узнайте свой chat_id:
   - Напишите боту `/start`
   - Откройте http://localhost:5000/api/telegram/test
   - Скопируйте ваш chat_id из `active_chats`

2. Добавьте в настройки `app/config/settings.py`:
```python
TELEGRAM_CHAT_IDS: str = "123456789"  # Ваш chat_id
# Или несколько через запятую:
TELEGRAM_CHAT_IDS: str = "123456789,987654321"
```

### Вариант 2: Проверьте токен бота

Убедитесь что токен правильный:
```python
# app/config/settings.py
TELEGRAM_BOT_TOKEN: str = "8588808155:AAHHfeIWRADTgRcOdSDI9byUHmzPJE2LGA8"
```

### Вариант 3: Проверьте что бот активен

1. Откройте @BotFather в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Убедитесь что бот не отключен

## ⚠️ Важно

- Бот должен быть запущен (это делается автоматически через BotFather)
- Пользователи должны написать боту хотя бы один раз
- Если бот не отправляет сообщения - проверьте токен в настройках

## 🔐 Безопасность

- Токен бота хранится в конфигурации (можно вынести в .env для production)
- Заявки сохраняются в БД даже если отправка в Telegram не удалась
- Ошибки логируются но не прерывают работу сайта

